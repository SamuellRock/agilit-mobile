{
  "name": "agilit-mobile",
  "main": "expo-router/entry",
  "version": "1.0.0",

  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "lint": "expo lint"
  },

  "dependencies": {
    // Expo 51 é a versão estável que mantém compatibilidade com RN 0.74 e React 18
    "expo": "~51.0.17",

    // React e React Native nas versões suportadas oficialmente pelo Expo 51
    "react": "18.2.0",
    "react-native": "0.74.5",

    // Expo Router 3 é a versão compatível com o ciclo do Expo 51
    "expo-router": "~3.4.8",

    // NativeWind 4 funciona com React Native 0.74 e Tailwind 3.4
    "nativewind": "4.0.36",
    "tailwindcss": "3.4.4",
    "postcss": "8.4.31",
    "autoprefixer": "10.4.16",

    // Versão de Reanimated compatível com Expo 51 e Gesture Handler 2.x
    "react-native-reanimated": "~3.10.1",

    // Gesture Handler compatível com RN 0.74 e Reanimated 3
    "react-native-gesture-handler": "~2.16.2",

    // Dependências essenciais para navegação interna do Expo Router
    "react-native-screens": "~3.31.1",
    "react-native-safe-area-context": "4.8.2",

    // Módulos Expo alinhados ao SDK 51
    "expo-font": "~12.0.8",
    "expo-image": "~1.0.6",
    "expo-web-browser": "~13.0.3",
    "expo-linking": "~5.0.2",
    "expo-constants": "~16.0.2",
    "expo-splash-screen": "~0.21.5",
    "expo-system-ui": "~2.0.3",
    "expo-status-bar": "~2.0.0",

    // Suporte ao React Native Web dentro do ecossistema Expo 51
    "react-native-web": "~0.19.10"
  },

  "devDependencies": {
    // Tipagens compatíveis com React 18
    "@types/react": "~18.2.67",

    // Typescript recomendado pela equipe do Expo para o SDK 51
    "typescript": "~5.2.2",

    // ESLint compatível com o preset do Expo
    "eslint": "^9.25.0",
    "eslint-config-expo": "~10.0.0"
  },

  "private": true
}
















babel.config.js

module.exports = function (api) {
  api.cache(true);
  return {
    presets: ["babel-preset-expo"],
    plugins: [
      "expo-router/babel",
      "nativewind/babel",
      "react-native-reanimated/plugin", // <-- Adicione esta linha
    ],
  };
};